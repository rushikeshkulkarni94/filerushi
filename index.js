const streamLib = require('./streamlib');
const fs = require('fs');

//streamLib.movefile();
//streamLib.myreaddir();
//streamLib.movefile();

streamLib.myreaddir();
streamLib.openfolder();
streamLib.movefile();